# Build context = ONLY what Dockerfile.spinalfmriprep COPYs. Everything else
# (esp. work/ = ~700G of derivatives) must never enter the Docker context.
*
!pyproject.toml
!README.md
!src
!policy
!schemas
!config
